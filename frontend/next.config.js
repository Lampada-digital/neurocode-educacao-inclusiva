/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Removemos qualquer "output: export" para o Vercel funcionar nativamente
}

module.exports = nextConfig
